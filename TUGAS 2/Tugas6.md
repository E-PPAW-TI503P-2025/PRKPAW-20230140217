## Request POST /register (untuk membuat user "mahasiswa")
![Request POST /register (untuk membuat user "mahasiswa")](ss/registermahasiswa.png)

## Request POST /register (untuk membuat user "admin").

![ Request POST /register (untuk membuat user "admin").](ss/registeradmin.png)

## Request POST /login (login sebagai "mahasiswa" dan mendapatkan token).
![Request POST /login (login sebagai "mahasiswa" dan mendapatkan token).](ss/loginmahasiswa.png)

## Database table users
![Database table users](ss/tableuser.png)

